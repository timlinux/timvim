{
  vim.utility = {
    preview = {
      glow = {
        enable = true;
        mappings = {
          openPreview = "<leader>mg";
        };
      };
      markdownPreview = {
        enable = true;
        alwaysAllowPreview = true;
        autoClose = true;
        autoStart = true;
      };

    };
  };
}
